@model IEnumerable<Marketim.Models.Arrear>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr
        <th>
           Müşteri Adı
        </th>
        <th>
           Borç Miktarı
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
       
        <td>
            @Html.DisplayFor(modelItem => item.CustomerName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SumArrear)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Arrearıd }) |
            @Html.ActionLink("Details", "Details", new { id=item.Arrearıd }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Arrearıd })
        </td>
    </tr>
}

</table>
